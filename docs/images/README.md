# Screenshots

Add PNGs here so the root **README** image embeds resolve on GitHub. Use the filenames referenced in the README screenshot table (for example `nextjs-01-library-ready.png`, `streamlit-01-grounded-sources.png`).

Optional extras: `web_empty.png`, `web_upload.png`, `web_syncing.png`, `web_diagnostics.png` (with `localStorage.KA_DEBUG=1`).
