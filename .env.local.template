# Copy this file to `.env.local` in the repository root (same folder as `.env.example`).
#
#   CMD:        copy .env.local.template .env.local
#   PowerShell: Copy-Item .env.local.template .env.local
#
# Then edit `.env.local` and replace the placeholder with your real OpenAI API key.
# File `.env.local` is gitignored — do not commit it.

OPENAI_API_KEY=your_openai_api_key_here

# Optional for this repo’s Python tools (eval, uvicorn) if your shell does not set it:
# PYTHONPATH=.
