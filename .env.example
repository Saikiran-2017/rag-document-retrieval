# Copy this file to ".env" and fill in your values.
# Command (PowerShell): Copy-Item .env.example .env

OPENAI_API_KEY=sk-your-openai-api-key-here

# Optional: max upload size in bytes for API + Streamlit saves (default ~50 MiB).
# KA_MAX_UPLOAD_BYTES=52428800
